specVersion: "v1beta"
serviceId: "resurent-management"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "resurent-management-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
